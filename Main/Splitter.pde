void splitter(PImage source){
  String offset = "";
  int[] key = new int[input.length()];
  for(int i = 0; i < input.length(); i++){
    int tempKey = 0;
  }
}

String offset(String x, int offset){
  return x.substring(offset)+ x.substring(0,offset);
}
